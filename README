# C++ Console Application.
Designed that application that takes a random word from a text file then searches through the same text file to find all words that are made up of the letters of that word. 
This required me to figure out reading from a separate file, searching each word individually, and checking if it’s the same.  
